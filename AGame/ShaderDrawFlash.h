#include "ShaderBase.h"

#pragma once
namespace Shader
{
#define DPROGRAM_COUNT 1
	class ShaderDrawFlash : public ShaderBase
	{
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLint mProjView[DPROGRAM_COUNT];
		GLint mWorld[DPROGRAM_COUNT];
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLint mDiffuse[DPROGRAM_COUNT];
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLint mCamPosition[DPROGRAM_COUNT];
		GLint mUpVec[DPROGRAM_COUNT];
		GLint mScaleAlpha[DPROGRAM_COUNT];
		////////////////////////////////////////////////////////////////////////////////////////////////
		uint32_t mTexSlot;
		////////////////////////////////////////////////////////////////////////////////////////////////
		void InitVariable(GLuint program);
		////////////////////////////////////////////////////////////////////////////////////////////////
	public:
		ShaderDrawFlash(Resource::ResourceBase *resourceLoad);
		~ShaderDrawFlash();
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLint GetProgram(void);
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLint GetPositionAttrib(void);
		GLint GetOffsetTexAttrib(void);
		////////////////////////////////////////////////////////////////////////////////////////////////
		void SetFlashTexture(void);
		////////////////////////////////////////////////////////////////////////////////////////////////
		void SetProjViewMatrix(glm::mat4 matrix);
		void SetWorldMatrix(glm::mat4 matrix);
		void SetScaleAlpha(glm::vec2 mScaleAlpha);
		////////////////////////////////////////////////////////////////////////////////////////////////
		void SetCamPos(glm::vec4 mCamPos);
		void SetUpVector(glm::vec4 mUp);
		////////////////////////////////////////////////////////////////////////////////////////////////
		void BindTextureToSlot(void);
		////////////////////////////////////////////////////////////////////////////////////////////////
	};
#undef DPROGRAM_COUNT
}

