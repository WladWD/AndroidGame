#include "MHeder.h"

#pragma once
namespace Collision
{
	class LowPolyModelBase
	{
		/////////////////////////////////////////////////////
		/////////////////////////////////////////////////////
	public:
		LowPolyModelBase();
		virtual ~LowPolyModelBase();
		/////////////////////////////////////////////////////
		virtual bool GetNextTriangle(glm::vec3 &mV0, glm::vec3 &mV1, glm::vec3 &mV2) = 0;
		/////////////////////////////////////////////////////
	};
}

