#include "LowPolyModelBase.h"

#pragma once
namespace Collision
{
	class LowPolyModelInstanceBase
	{
	protected:
		////////////////////////////////////////////////////////////////////
		glm::mat4 mWorld;
		////////////////////////////////////////////////////////////////////
		LowPolyModelBase *mLowpolyModel;
		////////////////////////////////////////////////////////////////////
	public:
		LowPolyModelInstanceBase(LowPolyModelBase *mLowpolyModel, glm::mat4 mWorld);
		virtual ~LowPolyModelInstanceBase();
		////////////////////////////////////////////////////////////////////
		void SetWorld(glm::mat4 mWorld);
		bool GetNextTriangle(glm::vec3 &mV0, glm::vec3 &mV1, glm::vec3 &mV2) const;
		////////////////////////////////////////////////////////////////////
		glm::mat4 GetWorldMatrix(void) const;
		////////////////////////////////////////////////////////////////////
	};
}

