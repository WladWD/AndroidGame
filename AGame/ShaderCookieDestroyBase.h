#include "ShaderBase.h"
#include "CookieDestroyStruct.h"

#pragma once
namespace Shader
{
	class ShaderCookieDestroyBase : public ShaderBase
	{
		////////////////////////////////////////////////////////////////////////////////////////////////
		virtual void InitVariable(GLuint program) = 0;
		////////////////////////////////////////////////////////////////////////////////////////////////
	public:
		ShaderCookieDestroyBase(uint32_t mProgramCount);
		virtual ~ShaderCookieDestroyBase();
		////////////////////////////////////////////////////////////////////////////////////////////////
		virtual void SetMovePices(glm::vec4 mVector[COOKIE_PICES_COUNT], uint32_t count) = 0;
		virtual void SetGlobalData(glm::vec2 mData) = 0;
		virtual void SetDrawPiceID(int32_t mID) = 0;
		////////////////////////////////////////////////////////////////////////////////////////////////
		virtual GLint GetPositionAttrib(void) = 0;
		//GLint GetNormalAttrib(void);
		virtual GLint GetTexCoordAttrib(void) = 0;
		////////////////////////////////////////////////////////////////////////////////////////////////
	};
}
