#include "MHeder.h"
#include "Mesh.h"

#pragma once
namespace FullScreenBuffer
{
	class FullScreenQuadBuffer
	{
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint vbBuffer, ibBuffer;
		////////////////////////////////////////////////////////////////////////////////////////////////
		void InitBuffer(void);
	public:
		FullScreenQuadBuffer(void);
		~FullScreenQuadBuffer();
		////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint GetArrayBuffer(void);
		GLuint GetElementArrayBuffer(void);
		////////////////////////////////////////////////////////////////////////////////////////////////
	};
}

